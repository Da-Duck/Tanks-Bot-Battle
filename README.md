# Танки. Битва Ботов

## 📋 Требования

- Python 3.7+
- Tkinter (обычно установлен с Python)

## 🚀 Запуск

### Основная игра

```bash
python main.py
```

Откроется окно настроек, где вы можете:
- Выбрать количество танков (2-8)
- Установить параметры поля (размер, препятствия)
- Настроить характеристики танков
- Включить сетевой режим (TCP)
- Запустить серию игр

**Пример содержимого botrandom.py:**

```python
import socket
import json
import random
import time
import sys

#задаем адрес сервера по умолчанию
host = "127.0.0.1"
port = 5000

#если указали адрес и порт в аргументах, используем их
if len(sys.argv) >= 3:
    host = sys.argv[1]
    port = int(sys.argv[2])

#создаем tcp соединение с сервером игры
sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
sock.connect((host, port))

#переключаемся на построчное чтение текста из сокета
text = sock.makefile("r", encoding="utf-8")

#читаем первую строку с приветствием от сервера
hello = text.readline().strip()

#пытаемся показать номер танка, если он есть
try:
    hellojson = json.loads(hello)
    tankid = hellojson.get("tank_id")
    if tankid is not None:
        print(f"мой танк: {tankid + 1}")
except json.JSONDecodeError:
    print("не удалось прочитать приветствие от сервера")

#основной цикл бота
while True:
    #читаем очередное состояние поля от сервера
    line = text.readline()
    if not line:
        #если сервер закрыл соединение, выходим
        print("соединение закрыто сервером")
        break

    line = line.strip()
    if not line:
        continue

    try:
        view = json.loads(line)
    except json.JSONDecodeError:
        #если пришло что-то непонятное, просто пропускаем
        continue

    #простая случайная тактика:
    #иногда поворачиваем, иногда едем, иногда стреляем
    command = ""

    #с небольшой вероятностью поворачиваем в случайную сторону
    if random.random() < 0.6:
        command += random.choice("lr")

    #с небольшой вероятностью едем вперед или назад
    if random.random() < 0.8:
        command += random.choice("fb")

    #если видим врагов и есть патроны, чаще жмем на выстрел
    enemies = view.get("enemies", [])
    ammo = view.get("self", {}).get("ammo", 0)
    if enemies and ammo > 0 and random.random() < 0.7:
        command += "s"
    elif random.random() < 0.1 and ammo > 0:
        command += "s"

    #если команда пустая, все равно немного дернемся вперед
    if not command:
        command = "f"

    #упаковываем команду в json и отправляем на сервер
    message = json.dumps({"cmd": command})
    try:
        sock.sendall((message + "\n").encode("utf-8"))
    except OSError:
        #если не удалось отправить команду, аккуратно выходим
        print("ошибка отправки команды, выходим")
        break

    #делаем небольшую паузу, чтобы не спамить сервер
    time.sleep(0.01)

#закрываем соединение и файловый объект
text.close()
sock.close()
```

**Запуск нескольких ботов:**

```bash
python my_bot.py 127.0.0.1 5000 &
python my_bot.py 127.0.0.1 5000 &
python my_bot.py 127.0.0.1 5000 &
```

### Использование встроенного API (apibot.py)

Удобный класс `TankGameAPI` для работы с игрой:

```bash
python apibot.py [host] [port]
```

**Структура:**

```python
from api import TankGameAPI
import time

#инициализация
api = TankGameAPI("127.0.0.1", 5000)
if not api.connect():
    print("Ошибка подключения")
    exit(1)

print(f"Я танк номер: {api.tank_id + 1}")

#основной цикл
while True:
    state = api.get_state()
    if state is None:
        break
    
    #Ваша логика здесь
    command = your_logic(state)
    api.command(command)
    
    time.sleep(0.02)

api.close()
```


## 📡 Формат данных (FOV - Field of View)

Каждый тик вы получаете JSON со следующей структурой:

```json
{
  "self": {
    "x": 100,
    "y": 100,
    "angle": 90,
    "health": 3,
    "ammo": 5
  },
  "enemies": [
    {
      "tank_id": 1,
      "x": 200,
      "y": 200,
      "angle": 180,
      "health": 2
    }
  ],
  "walls": [
    [100, 100, 150, 150]
  ],
  "water": [
    [300, 300, 350, 350]
  ],
  "dest": [
    [200, 200, 230, 230]
  ],
  "bullets": [
    {"x": 150, "y": 150}
  ],
  "map_width": 1280,
  "map_height": 720
}
```

### Методы TankGameAPI

- **connect()** - подключение к серверу (возвращает True/False)
- **get_state()** - получение текущего состояния (возвращает dict или None)
- **command(cmd_str)** - отправка команды (строка из l, r, f, b, s)
- **close()** - закрытие соединения

### Описание полей

- **self** - информация о вашем танке
  - `x, y` - координаты на поле
  - `angle` - угол направления (градусы, 0-360)
  - `health` - текущее здоровье
  - `ammo` - текущее количество боеприпасов

- **enemies** - список видимых врагов
  - Каждый враг содержит те же поля, что и self

- **walls** - неразрушаемые стены (прямоугольники [x1, y1, x2, y2])

- **water** - вода, через которую нельзя проехать

- **dest** - разрушаемые препятствия

- **bullets** - пули на поле, приближающиеся к вам

- **map_width, map_height** - размеры поля

## ⌨️ Команды управления

Отправляйте команды в виде строки (максимум 5 символов):

| Команда | Действие |
|---------|----------|
| `l` | Поворот влево |
| `r` | Поворот вправо |
| `f` | Движение вперед |
| `b` | Движение назад |
| `s` | Выстрел |

### Примеры команд

```python
"s"      # Только выстрел
"fs"     # Движение вперед + выстрел
"lr"     # Влево и вправо (взаимно исключают друг друга)
"lfs"    # Влево + вперед + выстрел
"rbs"    # Вправо + назад + выстрел
```

**Важно:** Команды l и r, f и b - взаимно исключают друг друга!

## 🎛️ Файлы проекта

- **main.py** - основной файл игры (Tkinter GUI, ядро логики)
- **api.py** - класс `TankGameAPI` для удобного подключения ботов
- **apibot.py** - пример бота с использованием API
- **botrandom.py** - пример простого бота с случайной стратегией
- **botrandom.exe** - exe бот


## 📊 Настройка сервера

В окне настроек основной игры:

- **🌐 IP адрес** - адрес, на котором слушает сервер (по умолчанию 0.0.0.0 для всех интерфейсов)
- **🔌 Порт** - TCP порт (по умолчанию 5000)
- **📡 Интервал сети** - как часто отправлять обновления ботам (мс)
- **⏱ Таймаут бота** - максимальное время ожидания команды от бота (мс)


## 🚦 Пошаговый запуск сетевой игры

1. **Запустите главное окно:**
   ```bash
   python main.py
   ```

2. **В окне настроек:**
   - Отметьте ✓ "Включить сеть (TCP)"
   - Установите IP (обычно 0.0.0.0) и порт (5000)
   - Выберите количество танков (например, 3)
   - Нажмите "СТАРТ СЕРИИ"

3. **В новом терминале запустите ботов (столько же, сколько выбрали):**
   ```bash
   # Терминал 1
   python botrandom.py 127.0.0.1 5000
   
   # Терминал 2
   python apibot.py 127.0.0.1 5000
   
   # Терминал 3
   python my_bot.py 127.0.0.1 5000
   ```

4. **Смотрите как они сражаются!**

## 💡 Советы по написанию бота

1. **Отслеживайте боеприпасы** - если `ammo == 0`, нельзя стрелять
2. **Избегайте препятствий** - используйте информацию о стенах и воде
3. **Ищите врагов** - проверяйте список `enemies`
4. **Управляйте углом** - поворачивайте к целям постепенно
5. **Не спамьте сервер** - используйте `time.sleep(0.02)` между командами
6. **Обрабатывайте ошибки** - проверяйте соединение


**Удачи в боях!**
